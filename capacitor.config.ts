import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'angular2apk',
  webDir: 'dist/angular2apk/browser'
};

export default config;
